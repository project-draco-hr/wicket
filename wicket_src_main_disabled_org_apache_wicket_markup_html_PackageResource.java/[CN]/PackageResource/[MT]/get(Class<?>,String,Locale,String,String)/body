{
  final SharedResources sharedResources=Application.get().getSharedResources();
  PackageResource resource=(PackageResource)sharedResources.get(scope,path,locale,style,variation,true);
  if (resource == null) {
    resource=newPackageResource(scope,path,locale,style,variation);
    Application.get().getSharedResources().add(scope,path,locale,style,variation,resource);
  }
  return resource;
}
