{
  TreeStateCache state=getTreeState();
  Object userObject=node.getTreeNode().getUserObject();
  TreePath selection=state.findTreePath(userObject);
  setExpandedState(selection,(!node.isExpanded()));
}
