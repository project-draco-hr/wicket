{
  if (resources != null) {
    while (resources.hasMoreElements()) {
      final URL url=resources.nextElement();
      URI uri=url.toURI();
      if (!loadedFiles.contains(uri)) {
        loadedFiles.add(uri);
      }
    }
  }
}
