{
  super.setUp();
  application=new MockWebApplication(null);
  application.getSettings().addStringResourceLoader(new BundleStringResourceLoader("wicket.model.StringResourceModelTest"));
  page=new MockPage(null);
  ws=new WeatherStation();
  wsModel=new Model(ws);
}
