{
  this.absolutePath=Packages.absolutePath(scope,path);
  this.scope=scope;
  this.path=path;
  this.locale=locale;
  this.style=style;
  if (locale != null) {
    getResourceStream();
    invalidate();
  }
}
