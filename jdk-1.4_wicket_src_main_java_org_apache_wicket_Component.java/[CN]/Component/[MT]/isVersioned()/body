{
  if (!getFlag(FLAG_VERSIONED) || !getFlag(FLAG_HAS_BEEN_RENDERED)) {
    return false;
  }
 else {
    if (parent != null) {
      if (!parent.isVersioned()) {
        return false;
      }
    }
    return true;
  }
}
