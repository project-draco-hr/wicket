{
  int idx=pageNumber;
  if (idx < 0) {
    idx=pageable.getPageCount() + idx;
  }
  if (idx > (pageable.getItemCount() - 1)) {
    idx=pageable.getItemCount() - 1;
  }
  if (idx < 0) {
    idx=0;
  }
  return idx;
}
