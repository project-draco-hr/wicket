{
  String id=getMarkupAttributes().getString("id");
  if (id == null) {
    id=getPageRelativePath();
    id=id.replace("_","__");
    id=id.replace(':','_');
    getMarkupAttributes().put("id",id);
  }
  return id;
}
