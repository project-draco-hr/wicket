{
  if (this.model != null) {
    this.model.detach();
  }
  if (this.model != model) {
    addStateChange(new ComponentModelChange(this.model));
    this.model=model;
  }
  modelChanged();
  return this;
}
