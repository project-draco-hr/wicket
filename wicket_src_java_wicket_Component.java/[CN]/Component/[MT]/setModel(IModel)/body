{
  if (model instanceof CompoundPropertyModel) {
    setFlag(FLAG_HAS_ROOT_MODEL,true);
  }
  if (this.model != null) {
    this.model.detach();
  }
  if (this.model != model) {
    modelChanging();
    this.model=(IModel)model;
    modelChanged();
  }
}
