{
  String id=getRequest().getParameter("behaviourId");
  if (id == null) {
    throw new WicketRuntimeException("parameter behaviourId was not provided: unable to locate listener");
  }
  int IdAsInt=Integer.parseInt(id);
  IBehaviourListener behaviourListener=(IBehaviourListener)behaviours.get(IdAsInt);
  if (behaviourListener == null) {
    throw new WicketRuntimeException("no behaviour listener found with behaviourId " + id);
  }
  behaviourListener.onRequest();
}
