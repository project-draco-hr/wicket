{
  String s;
  try {
    s="Page " + getPage();
  }
 catch (  IllegalStateException e) {
    s="";
  }
  if (!(this instanceof Page)) {
    s+=", component " + this;
  }
  return s + ": " + message;
}
