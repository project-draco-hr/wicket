{
  if (!getFlag(FLAG_VERSIONED) || (!getFlag(FLAG_IS_RENDERED_ONCE))) {
    return false;
  }
 else {
    if (parent != null) {
      if (!parent.isVersioned()) {
        return false;
      }
    }
    return true;
  }
}
