{
  if (!getApplication().getSecuritySettings().getAuthorizationStrategy().allowInstantiation(getClass())) {
    throw new UnauthorizedInstantiationException("insufficiently authorized to create component " + getClass());
  }
}
