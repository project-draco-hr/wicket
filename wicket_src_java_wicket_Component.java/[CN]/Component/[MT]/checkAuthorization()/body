{
  if (!getApplication().getAuthorizationStrategy().allowCreateComponent(getClass())) {
    throw new CreationNotAllowedException("insufficiently authorized to create component " + getClass());
  }
}
