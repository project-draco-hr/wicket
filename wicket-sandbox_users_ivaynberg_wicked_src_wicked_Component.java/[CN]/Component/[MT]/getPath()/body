{
  StringBuilder builder=new StringBuilder();
  for (Component c=this; c != null && c.parent != null; c=c.getParent()) {
    if (builder.length() > 0) {
      builder.insert(0,':');
    }
    builder.insert(0,c.getId());
  }
  return builder.toString();
}
