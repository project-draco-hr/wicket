{
  super(id);
  this.attribute=attribute;
  if (PackageResource.exists(clazz,href,getLocale(),getStyle(),getVariation())) {
    resourceReference=new ResourceReference(clazz,href,getLocale(),getStyle(),getVariation());
  }
 else {
    resourceReference=null;
  }
}
