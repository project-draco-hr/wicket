{
  if (contextPath != null) {
    if (!contextPath.startsWith("/") && !contextPath.startsWith("http:")) {
      this.contextPath="/" + contextPath;
    }
 else {
      this.contextPath=contextPath;
    }
  }
}
