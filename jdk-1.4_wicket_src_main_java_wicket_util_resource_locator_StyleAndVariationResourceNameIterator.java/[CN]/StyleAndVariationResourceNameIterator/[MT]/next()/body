{
  if (state == 0) {
    state++;
    if ((style != null) && (variation != null)) {
      return path + '_' + style+ '_'+ variation;
    }
  }
  if (state == 1) {
    state++;
    if (style != null) {
      return path + '_' + style;
    }
  }
  state=3;
  return path;
}
