{
  Session session=(Session)current.get();
  if (session == null) {
    final RequestCycle cycle=RequestCycle.get();
    session=Application.get().getSessionFactory().newSession(cycle.getRequest(),cycle.getResponse());
    set(session);
  }
  return session;
}
