{
  super.onConfigure();
  if (validator != null) {
    remove(validator);
    validator=null;
  }
  if (minimum != null || maximum != null) {
    validator=RangeValidator.range(minimum,maximum);
    add(validator);
  }
}
