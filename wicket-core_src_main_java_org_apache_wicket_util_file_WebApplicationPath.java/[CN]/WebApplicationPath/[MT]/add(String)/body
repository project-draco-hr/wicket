{
  final Folder folder=new Folder(path);
  if (folder.exists()) {
    super.add(folder);
  }
 else {
    if (!path.startsWith("/")) {
      path="/" + path;
    }
    if (!path.endsWith("/")) {
      path+="/";
    }
    webappPaths.add(path);
  }
}
