{
  int firstCell=listView.getCurrentPage() * listView.getRowsPerPage();
  String text=String.valueOf(listView.size()) + " items found, displaying " + String.valueOf(firstCell + 1)+ " to "+ String.valueOf(firstCell + listView.getRowsPerPage())+ ".";
  replaceComponentTagBody(markupStream,openTag,text);
}
