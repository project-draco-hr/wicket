{
  return neverRedirect(redirectPolicy) || (!ajax && ((onePassRender && notForcedRedirect(redirectPolicy)) || (targetEqualsCurrentUrl && notNewAndNotStatelessPage(newPageInstance,pageStateless)))) || (targetEqualsCurrentUrl && redirectToRender)|| shouldPreserveClientUrl;
}
