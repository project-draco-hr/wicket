{
  return redirectPolicy == RedirectPolicy.NEVER_REDIRECT || (onePassRender && ajax == false && redirectPolicy != RedirectPolicy.ALWAYS_REDIRECT) || (!ajax && (targetEqualsCurrentUrl && !newPageInstance && !pageStateless) || (targetEqualsCurrentUrl && redirectToRender)) || shouldPreserveClientUrl;
}
