{
  if (converterSupplier == null) {
    converterSupplier=getApplication().getApplicationSettings().getConverterSupplierFactory().newConverterSupplier();
  }
  return converterSupplier.getConverter(type);
}
