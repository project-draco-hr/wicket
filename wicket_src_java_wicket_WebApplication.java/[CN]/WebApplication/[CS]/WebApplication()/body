{
  settings=new ApplicationSettings(this);
  pages=new ApplicationPages();
  pages.setPageExpiredErrorPage(PageExpiredErrorPage.class).setInternalErrorPage(InternalErrorPage.class).setStaleDataErrorPage(StaleDataErrorPage.class);
}
