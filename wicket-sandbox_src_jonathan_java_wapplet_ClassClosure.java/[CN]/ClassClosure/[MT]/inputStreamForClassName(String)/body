{
  InputStream in=ClassLoader.getSystemClassLoader().getResourceAsStream(classname + ".class");
  if (in == null) {
    throw new IllegalStateException("Unable to find class " + classname);
  }
  return in;
}
