{
  if (!appletCodeClass.isAssignableFrom(IInitializer.class)) {
    throw new IllegalArgumentException("Applet code class for Applet must implement IAppletCode");
  }
  this.appletCodeClass=appletCodeClass;
}
