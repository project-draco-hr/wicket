{
  if (simpleName == null) {
    String pkg=getPackage();
    int i=pkg.length();
    String extName=getExternalName();
    if (i == 0)     simpleName=extName;
 else     simpleName=extName.substring(i + 1);
  }
  return simpleName;
}
