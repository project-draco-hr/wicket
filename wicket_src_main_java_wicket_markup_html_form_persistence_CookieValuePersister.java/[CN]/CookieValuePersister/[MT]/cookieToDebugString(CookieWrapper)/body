{
  return "[Cookie " + " name = " + cookie.getName() + ", value = "+ cookie.getValue()+ ", domain = "+ cookie.getCookie().getDomain()+ ", path = "+ cookie.getCookie().getPath()+ ", maxAge = "+ Time.valueOf(cookie.getCookie().getMaxAge()).toDateString()+ "("+ cookie.getCookie().getMaxAge()+ ")"+ "]";
}
