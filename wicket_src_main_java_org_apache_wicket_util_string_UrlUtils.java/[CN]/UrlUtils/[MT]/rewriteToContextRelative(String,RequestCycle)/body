{
  if (isRelative(url)) {
    System.out.println("TODO NG: Make sure this work properly");
    return requestCycle.getUrlRenderer().renderContextPathRelativeUrl(url,requestCycle.getRequest());
  }
 else {
    return url;
  }
}
