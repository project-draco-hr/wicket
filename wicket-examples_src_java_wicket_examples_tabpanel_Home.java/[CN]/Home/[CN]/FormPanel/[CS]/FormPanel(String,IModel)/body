{
  super(componentName);
  setModel(model);
  add(new Form("my_form",model,null){
    public void handleSubmit(){
      tab.toggleTab(((Integer)this.getModelObject()).intValue());
    }
  }
);
  add(new Label("my_label","This button will toggle between enabling and disabling the previous tab."));
}
