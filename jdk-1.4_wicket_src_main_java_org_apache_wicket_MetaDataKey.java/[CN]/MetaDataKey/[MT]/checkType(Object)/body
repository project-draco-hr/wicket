{
  Class<T> clazz=Classes.resolveClass(typeName);
  if (object != null && !clazz.isAssignableFrom(object.getClass())) {
    throw new IllegalArgumentException("MetaDataKey " + getClass() + " requires argument of "+ clazz+ ", not "+ object.getClass());
  }
}
