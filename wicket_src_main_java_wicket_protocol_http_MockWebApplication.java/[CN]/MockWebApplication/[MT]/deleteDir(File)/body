{
  if (dir != null && dir.isDirectory()) {
    File[] files=dir.listFiles();
    if (files != null) {
      for (      File element : files) {
        if (element.isDirectory()) {
          deleteDir(element);
        }
 else {
          element.delete();
        }
      }
    }
    dir.delete();
  }
}
