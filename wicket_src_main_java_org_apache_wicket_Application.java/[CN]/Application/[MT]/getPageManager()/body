{
  if (pageManager == null) {
synchronized (this) {
      if (pageManager == null) {
        pageManager=pageAccessSynchronizer.adapt(pageManagerProvider.get());
      }
    }
  }
  return pageManager;
}
