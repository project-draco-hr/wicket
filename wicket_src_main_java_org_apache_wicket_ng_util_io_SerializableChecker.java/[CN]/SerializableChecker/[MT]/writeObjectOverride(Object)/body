{
  if (!available) {
    return;
  }
  root=obj;
  if (fieldDescription == null) {
    fieldDescription=(root instanceof RequestableComponent) ? ((RequestableComponent)root).getPath() : "";
  }
  check(root);
}
