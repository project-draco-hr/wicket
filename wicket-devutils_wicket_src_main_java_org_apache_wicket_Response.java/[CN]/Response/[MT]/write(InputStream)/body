{
  OutputStream out=getOutputStream();
  try {
    Streams.copy(in,out);
  }
 catch (  Exception e) {
    handleException(e);
  }
 finally {
    try {
      in.close();
      out.flush();
    }
 catch (    IOException e) {
      handleException(e);
    }
  }
}
