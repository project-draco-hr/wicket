{
  final Component parentForm=component.findParent(Form.class);
  if (parentForm != null) {
    return parentForm.getName() + "." + component.getName()+ "."+ Classes.name(getClass());
  }
 else {
    throw new WicketRuntimeException("Unable to find Form parent for FormComponent " + component);
  }
}
