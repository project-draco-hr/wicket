{
  if (tag.isWicketHeadTag()) {
    Component component=new TransparentWebMarkupContainer(container,Component.AUTO_COMPONENT_PREFIX + tag.getId());
    component.setRenderBodyOnly(true);
    component.render(markupStream);
    return true;
  }
  return false;
}
