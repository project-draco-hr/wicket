{
  List<Method> listeners=ReflectionUtils.invocationChainForAnnotation(object.getClass(),annot,order.toClassOrder());
  for (  Method method : listeners) {
    invokeAnnotatedListenerMethod(object,method,annot);
  }
}
