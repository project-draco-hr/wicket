{
  return new TimerTask(){
    public void run(){
synchronized (resource) {
        if (log.isDebugEnabled()) {
          log.debug("Resource " + key + " flushed");
        }
        resource.invalidate();
      }
    }
  }
;
}
