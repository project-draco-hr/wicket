{
  this.dataStore=dataStore;
  entries=new LinkedBlockingQueue<Runnable>(capacity);
  savePagesExecutor=new ThreadPoolExecutor(MAX_THREADS,MAX_THREADS,1l,TimeUnit.SECONDS,entries,new RejectStoringTask());
}
