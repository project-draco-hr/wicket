{
  destroy.set(true);
synchronized (entries) {
    entries.notify();
  }
  try {
synchronized (destroy) {
      destroy.wait();
    }
  }
 catch (  InterruptedException e) {
    log.error(e.getMessage(),e);
  }
  dataStore.destroy();
}
