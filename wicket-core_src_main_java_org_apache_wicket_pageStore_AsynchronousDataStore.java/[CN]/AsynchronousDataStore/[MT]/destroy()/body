{
  destroy.set(true);
synchronized (entries) {
    entries.notify();
  }
  try {
synchronized (destroy) {
      destroy.wait();
    }
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
  dataStore.destroy();
}
