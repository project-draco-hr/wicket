{
  try {
    return Ognl.getValue(variableName,model).toString();
  }
 catch (  OgnlException e) {
    throw new WicketRuntimeException("Unable to get value of variable '" + variableName + "' in "+ string);
  }
}
