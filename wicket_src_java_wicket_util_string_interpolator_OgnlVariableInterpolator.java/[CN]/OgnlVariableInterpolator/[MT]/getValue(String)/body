{
  try {
    return Ognl.getValue(variableName,model).toString();
  }
 catch (  OgnlException e) {
    throw new RuntimeException("Unable to get value of variable '" + variableName + "' in "+ string);
  }
}
