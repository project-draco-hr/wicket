{
  if (root == null) {
    throw new NullPointerException("treeNode may not be null");
  }
  if (!(root instanceof MenuItem)) {
    throw new IllegalArgumentException("argument must be of type " + MenuItem.class.getName() + " (but is of type "+ root.getClass().getName()+ ")");
  }
  super.setRoot(root);
}
