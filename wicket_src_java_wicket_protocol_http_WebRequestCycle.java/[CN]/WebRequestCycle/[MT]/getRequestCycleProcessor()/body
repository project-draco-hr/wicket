{
  if (requestCycleProcessor == null) {
    requestCycleProcessor=new CompoundRequestCycleProcessor(new WebTargetResolverStrategy(),new WebEventProcessorStrategy());
  }
  return requestCycleProcessor;
}
