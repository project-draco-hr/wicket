{
  notify(new INotifier<IChangeListener>(){
    @Override public void notify(    final IChangeListener listener){
      if (listener instanceof IChangeListener2) {
        ((IChangeListener2)listener).onChange(modifiable);
      }
 else {
        listener.onChange();
      }
    }
  }
);
}
