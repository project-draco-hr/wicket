{
  List serialized=serializePage(page);
  for (Iterator i=serialized.iterator(); i.hasNext(); ) {
    SerializedPage serializedPage=(SerializedPage)i.next();
    savePageData(sessionId,serializedPage);
  }
}
