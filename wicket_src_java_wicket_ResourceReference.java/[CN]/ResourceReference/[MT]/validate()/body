{
  if (getResource() instanceof PackageResource) {
    if (!PackageResource.exists(scope,name,locale,style)) {
      throw new IllegalStateException("resource [scope=" + scope + ",name="+ name+ ",locale="+ locale+ "style="+ style+ "] not found");
    }
  }
}
