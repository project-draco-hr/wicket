{
  final String[] paramTuples=queryString.split("&");
  for (int t=0; t < paramTuples.length; t++) {
    final String[] bits=paramTuples[t].split("=");
    if (bits.length == 2) {
      params.addNamedParameter(WicketURLDecoder.QUERY_INSTANCE.decode(bits[0],getCurrentCharset()),WicketURLDecoder.QUERY_INSTANCE.decode(bits[1],getCurrentCharset()));
    }
 else {
      params.addNamedParameter(WicketURLDecoder.QUERY_INSTANCE.decode(bits[0],getCurrentCharset()),"");
    }
  }
}
