{
  if (webSocketSettings.isHijackingProtectionEnabled()) {
    String oUrl=getOriginUrl(servletRequest);
    if (invalid(oUrl))     throw new ConnectionRejectedException(POLICY_VIOLATION,ORIGIN_MISMATCH);
  }
}
