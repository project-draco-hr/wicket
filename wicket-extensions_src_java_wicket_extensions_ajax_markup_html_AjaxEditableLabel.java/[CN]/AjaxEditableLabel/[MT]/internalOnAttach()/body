{
  super.onBeforeRender();
  if (tempModel != null) {
    label=newLabel(this,"label",tempModel);
    editor=newEditor(this,"editor",tempModel);
    add(label);
    add(editor);
    this.tempModel=null;
  }
}
