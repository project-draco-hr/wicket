{
  super(parent,id);
  IModel<T> m=getModel();
  if (m == null) {
    throw new IllegalStateException("No model found for this component, either pass one explicitly or " + "make sure an inheritable model is available");
  }
  init(m);
}
