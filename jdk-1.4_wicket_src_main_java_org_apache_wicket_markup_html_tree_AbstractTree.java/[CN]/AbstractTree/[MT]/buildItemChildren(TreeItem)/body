{
  List items;
  if (isNodeExpanded((TreeNode)item.getModelObject())) {
    items=buildTreeItems(nodeChildren((TreeNode)item.getModelObject()),item.getLevel() + 1);
  }
 else {
    items=Collections.EMPTY_LIST;
  }
  item.setChildren(items);
}
