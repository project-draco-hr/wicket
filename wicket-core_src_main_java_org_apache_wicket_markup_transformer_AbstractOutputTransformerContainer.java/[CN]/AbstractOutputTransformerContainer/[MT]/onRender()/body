{
  if (transformBodyOnly == false) {
    execute(new Runnable(){
      public void run(){
        AbstractOutputTransformerContainer.super.onRender();
      }
    }
);
  }
 else {
    super.onRender();
  }
}
