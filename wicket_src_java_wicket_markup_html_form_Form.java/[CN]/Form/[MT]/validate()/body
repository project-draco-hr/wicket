{
  getValidationStrategy().validate(this);
  if (hasError()) {
    invalid();
    onError();
    return false;
  }
 else {
    updateFormComponentModels();
    persistFormComponentData();
    onSubmit();
    return true;
  }
}
