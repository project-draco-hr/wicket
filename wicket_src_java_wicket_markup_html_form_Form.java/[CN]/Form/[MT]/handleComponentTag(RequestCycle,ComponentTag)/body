{
  checkTag(tag,"form");
  super.handleComponentTag(cycle,tag);
  tag.put("method","POST");
  String url=cycle.urlFor(Form.this,IFormSubmitListener.class);
  url=url.replaceAll("&","&amp;");
  tag.put("action",url);
}
