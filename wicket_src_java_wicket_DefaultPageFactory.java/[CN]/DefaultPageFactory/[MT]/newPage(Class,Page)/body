{
  Constructor constructor=constructor(pageClass,Page.class);
  if (constructor != null) {
    return newPage(constructor,page);
  }
  throw new WicketRuntimeException("Could not find a constructor in " + pageClass + " that would accept Page argument "+ page);
}
