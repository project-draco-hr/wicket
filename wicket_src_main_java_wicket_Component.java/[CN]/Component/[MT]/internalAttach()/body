{
  onAttach();
  internalOnAttach();
  List<Method> listeners=ReflectionUtils.invocationChainForAnnotation(getClass(),OnAttach.class,ClassOrder.SUPER_TO_SUB);
  for (  Method method : listeners) {
    invokeAnnotatedListenerMethod(method,OnAttach.class);
  }
}
