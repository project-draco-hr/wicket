{
  internalOnDetach();
  onDetach();
  List<Method> listeners=ReflectionUtils.invocationChainForAnnotation(getClass(),OnDetach.class,ClassOrder.SUB_TO_SUPER);
  for (  Method method : listeners) {
    invokeAnnotatedListenerMethod(method,OnAttach.class);
  }
}
