{
  if (resourceReference != null) {
    getApplication().getResourceReferenceRegistry().registerResourceReference(resourceReference);
    return getRequestCycle().renderUrlFor(new ResourceReferenceRequestHandler(resourceReference,resourceParameters));
  }
  return urlFor(IResourceListener.INTERFACE);
}
