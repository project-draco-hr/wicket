{
  super(parent,id);
  username=new TextField(this,"username",new PropertyModel(properties,"username"));
  password=new PasswordTextField(this,"password",new PropertyModel(properties,"password"));
  WebMarkupContainer rememberMeRow=new WebMarkupContainer(this,"rememberMeRow");
  new CheckBox(rememberMeRow,"rememberMe",new PropertyModel(SignInPanel.this,"rememberMe"));
  setPersistent(rememberMe);
  rememberMeRow.setVisible(includeRememberMe);
}
