{
  Bean bean=new Bean();
  ctx.putBean("bean",bean);
  SpringBeanLocator locator=(SpringBeanLocator)WicketObjects.cloneObject(new SpringBeanLocator("bean",Bean.class,ctxLocator));
  assertNotNull(locator.locateProxyTarget());
}
