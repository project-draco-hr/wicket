{
  assertThat(wrx.getCause().getMessage(),is(equalTo("Markup id set on a component that is usually not " + "rendered into markup. Markup id: test1, component id: test, " + "component tag: wicket:label.")));
}
