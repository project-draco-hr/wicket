{
  super(componentName,feedback);
  add(new TextField("username",new PropertyModel(new Model(properties),"username")));
  add(new PasswordTextField("password",new PropertyModel(new Model(properties),"password")));
}
