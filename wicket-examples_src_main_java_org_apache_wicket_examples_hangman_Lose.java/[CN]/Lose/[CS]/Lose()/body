{
  add(new Label("currentWord",getGame().getWord().asString()));
  add(new Link("playAgain"){
    public void onClick(){
      getGame().newGame();
      setResponsePage(Guess.class);
    }
  }
);
}
