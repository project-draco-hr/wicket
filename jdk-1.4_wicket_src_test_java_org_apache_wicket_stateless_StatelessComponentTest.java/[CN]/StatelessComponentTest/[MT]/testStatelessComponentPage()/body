{
  executeTest(StatelessComponentPage.class,"StatelessComponentPage_result.html");
  tester.setupRequestAndResponse();
  tester.getServletRequest().setURL("?wicket:bookmarkablePage=:org.apache.wicket.stateless.StatelessComponentPage&wicket:interface=:0:link::ILinkListener:");
  try {
    tester.processRequestCycle();
    assertTrue(false);
  }
 catch (  Exception e) {
    assertEquals("wanted exception",e.getMessage());
  }
}
