{
  super.init();
  new CdiConfiguration().setPropagation(ConversationPropagation.NONBOOKMARKABLE).configure(this);
  mountPage("injection",InjectionPage.class);
  mountPage("conversation",ConversationPage1.class);
}
