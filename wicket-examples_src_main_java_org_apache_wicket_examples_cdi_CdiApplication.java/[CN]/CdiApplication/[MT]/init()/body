{
  super.init();
  CdiConfiguration.get().setPropagation(ConversationPropagation.NONBOOKMARKABLE).configure(this);
  mountPage("injection",InjectionPage.class);
  mountPage("conversation",ConversationPage1.class);
}
