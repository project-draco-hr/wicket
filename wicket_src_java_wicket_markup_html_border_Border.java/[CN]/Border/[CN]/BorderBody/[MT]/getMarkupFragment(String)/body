{
  MarkupFragment fragment=Border.this.getMarkupFragment(id);
  if (fragment != null) {
    return fragment;
  }
  return Border.this.getAssociatedMarkup(true).getWicketFragment(BORDER,true).getChildFragment(id,true);
}
