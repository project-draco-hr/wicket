{
  if (tag.isWicketBodyTag()) {
    return false;
  }
  final Component child=getParent().get(tag.getId());
  if (child != null) {
    child.render(markupStream);
    return true;
  }
  return false;
}
