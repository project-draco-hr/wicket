{
  if (index >= size)   throw new IndexOutOfBoundsException("Index: " + index + ", Size: "+ size);
  Object oldValue=childs[index];
  int numMoved=size - index - 1;
  if (numMoved > 0)   System.arraycopy(childs,index + 1,childs,index,numMoved);
  childs[--size]=null;
  return oldValue;
}
