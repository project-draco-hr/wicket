{
  byte data[]=loadPageData(defaultWorkDir,sessionId,pageMapName,pageId,versionNumber,ajaxVersionNumber);
  if (data != null) {
    return deserializePage(data,versionNumber);
  }
 else {
    return null;
  }
}
