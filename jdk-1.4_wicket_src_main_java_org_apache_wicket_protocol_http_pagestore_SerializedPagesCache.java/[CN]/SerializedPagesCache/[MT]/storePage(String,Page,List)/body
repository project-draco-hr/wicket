{
  SerializedPageWithSession entry=new SerializedPageWithSession(sessionId,page,pagesList);
  if (size > 0) {
synchronized (cache) {
      removePage(page);
      cache.add(entry);
      if (cache.size() > size) {
        cache.remove(0);
      }
    }
  }
  return entry;
}
