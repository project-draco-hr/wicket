{
  super.setUp();
  application=new MockHttpApplication(null);
  application.getSettings().addStringResourceLoader(new BundleStringResourceLoader("wicket.StringResourceModelTest"));
  page=new MockPage(null);
  ws=new WeatherStation();
  wsModel=new Model(ws);
}
