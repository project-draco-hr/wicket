{
  for (  Component component : children) {
    if (component == body) {
      super.addOrReplace(component);
    }
 else {
      getBodyContainer().addOrReplace(component);
    }
  }
  return this;
}
