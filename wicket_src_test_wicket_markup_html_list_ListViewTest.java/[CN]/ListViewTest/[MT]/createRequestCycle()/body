{
  application.setupRequestAndResponse();
  WebRequestCycle cycle=new WebRequestCycle(application,application.getWicketSession(),application.getWicketRequest(),application.getWicketResponse());
  MockPage page=new MockPage(null);
  cycle.setResponsePage(page);
  return cycle;
}
