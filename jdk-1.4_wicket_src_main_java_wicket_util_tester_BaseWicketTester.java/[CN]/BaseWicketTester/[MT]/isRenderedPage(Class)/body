{
  if (!getLastRenderedPage().getClass().isAssignableFrom(expectedRenderedPageClass)) {
    return isEqual(Classes.simpleName(expectedRenderedPageClass),Classes.simpleName(getLastRenderedPage().getClass()));
  }
  return Result.pass();
}
