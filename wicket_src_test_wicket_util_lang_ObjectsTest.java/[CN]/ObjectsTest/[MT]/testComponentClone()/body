{
  PropertyModel pm=new PropertyModel(new TextField<String>(new EmptyPage(),"test",new Model<String>("test")),"modelObject");
  PropertyModel pm2=(PropertyModel)Objects.cloneModel(pm);
  assertTrue(pm.getObject() == pm2.getObject());
}
