{
  javax.servlet.http.HttpSession httpSession=((HttpSession)session).getHttpServletSession();
  return WebApplicationContextUtils.getRequiredWebApplicationContext(httpSession.getServletContext());
}
