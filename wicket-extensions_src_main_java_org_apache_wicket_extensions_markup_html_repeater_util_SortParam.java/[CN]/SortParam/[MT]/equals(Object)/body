{
  if (this == o) {
    return true;
  }
  if (!(o instanceof SortParam)) {
    return false;
  }
  @SuppressWarnings("unchecked") SortParam<T> sortParam=(SortParam<T>)o;
  return (ascending == sortParam.ascending) && property.equals(sortParam.property);
}
