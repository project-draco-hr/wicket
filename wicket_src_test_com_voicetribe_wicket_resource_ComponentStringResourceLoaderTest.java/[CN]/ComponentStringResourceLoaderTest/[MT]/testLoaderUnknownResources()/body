{
  Component c=new DummyComponent("hello",application){
  }
;
  DummyPage page=new DummyPage();
  page.add(c);
  IStringResourceLoader loader=new ComponentStringResourceLoader();
  Assert.assertNull("Missing resource should return null",loader.get(c,"test.string",Locale.getDefault(),null));
}
