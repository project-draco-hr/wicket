{
  if (resources != null) {
    while (resources.hasMoreElements()) {
      final URL url=(URL)resources.nextElement();
      if (!loadedFiles.contains(url)) {
        loadedFiles.add(url);
      }
    }
  }
}
