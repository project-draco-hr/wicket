{
  final String parametersFragment=requestParameters.getPath().substring(getMountPath().length());
  final PageParameters parameters=new PageParameters(decodeParameters(parametersFragment,requestParameters.getParameters()));
  final String pageMapName=(String)parameters.remove(WebRequestCodingStrategy.PAGEMAP);
  requestParameters.setPageMapName(pageMapName);
  final String bookmarkableFormName=(String)parameters.remove(WebRequestCodingStrategy.BOOKMARKABLE_FORM_PARAMETER_NAME);
  final BookmarkablePageRequestTarget target;
  if (bookmarkableFormName != null) {
    requestParameters.setBookmarkableFormName(bookmarkableFormName);
    target=new BookmarkableFormPageRequestTarget(pageMapName,bookmarkablePageClass,parameters,bookmarkableFormName);
  }
 else {
    target=new BookmarkablePageRequestTarget(pageMapName,bookmarkablePageClass,parameters);
  }
  return target;
}
