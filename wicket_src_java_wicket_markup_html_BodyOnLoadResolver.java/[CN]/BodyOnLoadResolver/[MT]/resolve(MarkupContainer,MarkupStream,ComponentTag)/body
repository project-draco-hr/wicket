{
  if ((tag instanceof ComponentTag) && "body".equalsIgnoreCase(tag.getName()) && (tag.getNamespace() == null)) {
    BodyOnLoadContainer body=new BodyOnLoadContainer();
    container.autoAdd(body);
    return true;
  }
  return false;
}
