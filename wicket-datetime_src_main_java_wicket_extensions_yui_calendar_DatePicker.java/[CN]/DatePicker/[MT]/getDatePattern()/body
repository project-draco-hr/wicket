{
  if (component instanceof ITextFormatProvider) {
    return ((ITextFormatProvider)component).getTextFormat();
  }
 else {
    DateConverter dateConverter=(DateConverter)((Converter)component.getConverter()).get(Date.class);
    return ((SimpleDateFormat)dateConverter.getDateFormat(component.getLocale())).toPattern();
  }
}
