{
  if (!(component instanceof IDatePatternProvider)) {
    throw new WicketRuntimeException("this behavior can only be added to components that implement " + IDatePatternProvider.class.getName());
  }
  component.setOutputMarkupId(true);
  this.component=component;
}
