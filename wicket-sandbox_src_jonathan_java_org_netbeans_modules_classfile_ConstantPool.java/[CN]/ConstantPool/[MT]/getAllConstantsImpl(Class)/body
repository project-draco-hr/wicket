{
  int n=cpEntries.length;
  Collection c=new ArrayList(n);
  for (int i=CONSTANT_POOL_START; i < n; i++) {
    if (cpEntries[i] != null && cpEntries[i].getClass().equals(classType)) {
      c.add(cpEntries[i]);
    }
  }
  return c;
}
