{
  super.onConfigure();
  if (validator != null) {
    remove(validator);
  }
  Double min=minimum;
  Double max=maximum;
  if (min == null) {
    min=Double.MIN_VALUE;
  }
  if (max == null) {
    max=Double.MAX_VALUE;
  }
  validator=new RangeValidator<Double>(min,max);
  add(validator);
}
