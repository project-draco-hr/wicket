{
  if (url == null) {
    return false;
  }
 else {
    List<String> urlSegments=url.getSegments();
    if (urlSegments.size() < segments.length) {
      return false;
    }
 else {
      for (int i=0; i < segments.length; ++i) {
        if ((segments[i].equals(urlSegments.get(i)) == false) && (getPlaceholder(segments[i]) == null && getOptionalPlaceholder(segments[i]) == null)) {
          return false;
        }
      }
    }
  }
  return true;
}
