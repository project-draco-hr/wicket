{
  if (url == null) {
    return false;
  }
 else {
    if (url.getSegments().size() < segments.length) {
      return false;
    }
 else {
      for (int i=0; i < segments.length; ++i) {
        if ((segments[i].equals(url.getSegments().get(i)) == false) && (getPlaceholder(segments[i]) == null)) {
          return false;
        }
      }
    }
  }
  return true;
}
