{
  ApplicationSettings settings=new ApplicationSettings(new DummyApplication());
  Assert.assertTrue("useDefaultOnMissingResource should default to true",settings.isUseDefaultOnMissingResource());
}
