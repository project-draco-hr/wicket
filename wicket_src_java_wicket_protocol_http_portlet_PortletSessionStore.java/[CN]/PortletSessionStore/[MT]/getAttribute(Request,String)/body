{
  WicketPortletRequest webRequest=toPortletRequest(request);
  PortletSession httpSession=getPortletSession(webRequest);
  if (httpSession != null) {
    return httpSession.getAttribute(getSessionAttributePrefix(webRequest) + name,PortletSession.PORTLET_SCOPE);
  }
  return null;
}
