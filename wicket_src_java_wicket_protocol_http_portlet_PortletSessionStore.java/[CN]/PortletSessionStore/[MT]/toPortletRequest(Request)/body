{
  if (request == null) {
    return null;
  }
  if (!(request instanceof WicketPortletRequest)) {
    throw new IllegalArgumentException(getClass().getName() + " can only work with WicketPortletRequest");
  }
  return (WicketPortletRequest)request;
}
