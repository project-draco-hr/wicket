{
  getForm().getRootForm().onFormSubmitted();
  if (!getForm().isSubmitted()) {
    return;
  }
  if (!getForm().hasError()) {
    onSubmit(target);
  }
  if (form.findParent(Page.class) != null) {
    if (getForm().hasError()) {
      onError(target);
    }
  }
}
