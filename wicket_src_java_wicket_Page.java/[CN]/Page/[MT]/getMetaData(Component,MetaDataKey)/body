{
  if (metaData != null) {
    for (int i=0; i < metaData.length; i++) {
      MetaDataEntry m=metaData[i];
      if (component == m.component && key.equals(m.key)) {
        return m.object;
      }
    }
  }
  return null;
}
