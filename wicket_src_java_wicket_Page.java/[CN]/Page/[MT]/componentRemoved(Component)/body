{
  if (!component.isAuto()) {
    checkModification();
    if (getFlag(FLAG_TRACK_CHANGES) && component.isVersioned()) {
      newVersion();
      versionManager.componentRemoved(component);
    }
  }
}
