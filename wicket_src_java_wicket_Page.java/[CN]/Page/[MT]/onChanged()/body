{
  if (isVersioned) {
    if (trackChanges) {
      if (versionManager == null) {
        versionManager=newVersionManager();
      }
      if (changeCount == 0) {
        versionManager.beginVersion();
      }
      changeCount++;
    }
  }
}
