{
  RequestCycle requestCycle=getRequestCycle();
  String url=requestCycle.getProcessor().getRequestCodingStrategy().encode(requestCycle,new SharedResourceRequestTarget(resourceKey));
  return url;
}
