{
  ComponentTag tag=(ComponentTag)getParent().nextTag();
  if (tag == null) {
    return tag;
  }
  if (tag.isOpen() && "body".equalsIgnoreCase(tag.getName())) {
    if (tag.getId() == null) {
      tag.setId("_body");
    }
  }
  return tag;
}
