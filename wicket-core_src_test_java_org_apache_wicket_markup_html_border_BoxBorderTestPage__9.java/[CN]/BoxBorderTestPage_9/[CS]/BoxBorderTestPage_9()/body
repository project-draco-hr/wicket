{
  Border myBorder=new BorderComponent1("myBorder");
  add(myBorder);
  Fragment panel1=new Fragment("fragmentsWillBeRenderedHere","fragmentSource",myBorder.getBodyContainer());
  myBorder.add(panel1);
}
