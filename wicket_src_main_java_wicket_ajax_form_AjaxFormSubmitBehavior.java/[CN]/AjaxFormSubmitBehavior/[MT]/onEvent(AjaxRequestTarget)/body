{
  final Form<?> form=getForm().getRootForm();
  form.onFormSubmitted();
  if (!form.hasError()) {
    onSubmit(target);
  }
  if (form.findParent(Page.class) != null) {
    if (getForm().hasError()) {
      onError(target);
    }
  }
}
