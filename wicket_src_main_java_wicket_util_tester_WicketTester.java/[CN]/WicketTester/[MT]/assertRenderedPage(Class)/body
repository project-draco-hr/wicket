{
  if (!getLastRenderedPage().getClass().isAssignableFrom(expectedRenderedPageClass)) {
    Assert.assertEquals(Classes.simpleName(expectedRenderedPageClass),Classes.simpleName(getLastRenderedPage().getClass()));
  }
}
