{
  if (!getLastRenderedPage().getClass().isAssignableFrom(expectedReneredPageClass)) {
    Assert.assertEquals(Classes.simpleName(expectedReneredPageClass),Classes.simpleName(getLastRenderedPage().getClass()));
  }
}
