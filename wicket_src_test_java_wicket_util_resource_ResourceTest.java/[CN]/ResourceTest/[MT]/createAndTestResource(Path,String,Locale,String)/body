{
  IResourceStreamFactory locator=new ResourceStreamFactory(sourcePath);
  IResourceStream resource=locator.newResourceStream(this.getClass(),this.getClass().getName().replace('.','/'),style,locale,"txt");
  compareFilename(resource,extension);
}
