{
  log.info("registering component for {} for session {}: {}",new Object[]{page.getPageId(),Session.get().getId(),subscription.getComponentPath()});
  subscriptions.put(new PageKey(page.getPageId(),Session.get().getId()),subscription);
}
