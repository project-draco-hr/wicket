{
  FormComponent fc=getFormComponent();
  fc.registerNewUserInput();
  fc.validate();
  if (fc.hasErrorMessage()) {
    fc.invalid();
  }
 else {
    fc.valid();
    fc.updateModel();
  }
  onUpdate(target);
}
