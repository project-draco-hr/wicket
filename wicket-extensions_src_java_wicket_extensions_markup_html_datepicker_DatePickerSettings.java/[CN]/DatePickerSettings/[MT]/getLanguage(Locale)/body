{
  if (language != null) {
    return language;
  }
  StaticResourceReference ref=(StaticResourceReference)localeToLanguageReference.get(currentLocale.getLanguage());
  if (ref != null) {
    return ref;
  }
  return LANGUAGE_EN;
}
