{
  String id=tag.getId();
  if (!id.equals(BODY_ID)) {
    Component component=get(id);
    if (component != null) {
      component.render(markupStream);
      return true;
    }
  }
  return false;
}
