{
  MarkupResourceStream stream=new MarkupResourceStream(new StringResourceStream(markup),null,null);
  final MarkupParser parser=new MarkupParserFactory(this.tester.getApplication()).newMarkupParser(stream);
  return parser.readAndParse();
}
