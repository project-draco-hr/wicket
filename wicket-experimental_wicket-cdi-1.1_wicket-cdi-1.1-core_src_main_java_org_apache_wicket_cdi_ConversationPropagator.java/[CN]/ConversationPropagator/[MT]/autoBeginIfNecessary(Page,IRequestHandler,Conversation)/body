{
  if (!auto.get() || conversation == null || !conversation.isTransient() || page == null || !propagationSource.get().propagatesViaPage(page,handler) || !hasConversationalComponent(page)) {
    return;
  }
  conversation.begin();
  logger.debug("Auto-began conversation {} for page {}",conversation.getId(),page);
}
