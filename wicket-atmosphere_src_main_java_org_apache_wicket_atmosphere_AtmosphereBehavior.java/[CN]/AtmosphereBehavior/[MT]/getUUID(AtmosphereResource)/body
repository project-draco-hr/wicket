{
  String trackingId=resource.getRequest().getHeader(HeaderConfig.X_ATMOSPHERE_TRACKING_ID);
  if (trackingId != null)   return trackingId;
  return resource.getRequest().getHeader("Sec-WebSocket-Key");
}
