{
  if (value == null) {
    return null;
  }
  if (c == CONVERT_TO_DEFAULT_TYPE || Number.class.isAssignableFrom(c) || c == Double.TYPE) {
    String pattern=getPattern();
    Number temp=getNumber(value);
    return (temp instanceof Double) ? (Double)temp : new Double(temp.doubleValue());
  }
 else   if (String.class.isAssignableFrom(c)) {
    return toString(value);
  }
  throw new ConversionException(this + " cannot handle conversions of type " + c);
}
