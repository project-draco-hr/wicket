{
  if (metaData != null) {
    for (int i=0; i < metaData.length; i++) {
      MetaDataEntry m=metaData[i];
      if (key.equals(m.key)) {
        return m.object;
      }
    }
  }
  return null;
}
