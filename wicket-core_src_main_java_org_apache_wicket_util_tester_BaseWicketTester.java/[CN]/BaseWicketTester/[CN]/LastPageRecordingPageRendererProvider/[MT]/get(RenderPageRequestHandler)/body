{
  Page newPage=(Page)handler.getPageProvider().getPageInstance();
  if (startComponent != null && lastPage != null && lastPage.getPageClass() != newPage.getPageClass()) {
    startComponent=null;
  }
  lastRenderedPage=lastPage=newPage;
  return delegate.get(handler);
}
