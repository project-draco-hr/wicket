{
  String result="new Array(";
  for (int i=0; i < results.length; i++) {
    result+="'" + results[i] + "'";
    if (i < results.length - 1) {
      result+=",";
    }
  }
  result+=")";
  return result;
}
