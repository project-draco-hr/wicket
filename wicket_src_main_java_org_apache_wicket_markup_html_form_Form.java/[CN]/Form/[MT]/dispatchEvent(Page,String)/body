{
  Url parsed=Url.parse(url,getRequest().getCharset());
  IRequestHandler handler=getApplication().getRootRequestMapper().mapRequest(getRequest().requestWithUrl(parsed));
  if (handler != null) {
    getRequestCycle().scheduleRequestHandlerAfterCurrent(handler);
  }
}
