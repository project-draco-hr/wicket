{
  super(threshold);
  if (fileFactory == null)   throw new IllegalArgumentException("FileFactory must be specified");
  this.fileFactory=fileFactory;
  memoryOutputStream=new ByteArrayOutputStream();
  currentOutputStream=memoryOutputStream;
}
