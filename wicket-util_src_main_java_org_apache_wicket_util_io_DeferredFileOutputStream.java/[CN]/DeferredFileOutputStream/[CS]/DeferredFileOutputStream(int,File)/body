{
  super(threshold);
  if (outputFile == null)   throw new IllegalArgumentException("output file must be specified");
  this.outputFile=outputFile;
  fileFactory=null;
  memoryOutputStream=new ByteArrayOutputStream();
  currentOutputStream=memoryOutputStream;
}
