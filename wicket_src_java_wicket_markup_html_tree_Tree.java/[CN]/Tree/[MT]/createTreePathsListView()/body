{
  final TreeState treeState=getTreeState();
  this.treePathList=new ArrayList();
  addNodesToTreePathList();
  final TreePathsListView treePaths=new TreePathsListView("tree",new Model((Serializable)treePathList));
  return treePaths;
}
