{
  removeAll();
  List visiblePathsList=new ArrayList();
  TreePath selectedPath=getTreeState().getSelectedPath();
  Enumeration e=getTreeState().getVisiblePathsFromRoot();
  while (e.hasMoreElements()) {
    visiblePathsList.add(e.nextElement());
  }
  List nestedList=new ArrayList();
  buildList(visiblePathsList,0,0,nestedList);
  add(getTreeRowsPanel("tree",nestedList));
}
