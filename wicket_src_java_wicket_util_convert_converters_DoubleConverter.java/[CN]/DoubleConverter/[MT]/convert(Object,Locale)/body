{
  final Number number=value instanceof Number ? (Number)value : parse(value,Double.MAX_VALUE * -1,Double.MAX_VALUE,locale);
  if (number == null) {
    return null;
  }
  return new Double(number.doubleValue());
}
