{
  if (request.getUrl().getSegments().size() == 0 && request.getUrl().getQueryParameters().size() == 0) {
    return new RenderPageRequestHandler(new DefaultPageProvider(getContext().getHomePageClass()));
  }
 else {
    return null;
  }
}
