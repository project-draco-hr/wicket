{
  return new Statement(){
    @Override public void evaluate() throws Throwable {
      tester=new WicketTester();
      setup(tester);
      try {
        base.evaluate();
      }
  finally {
        tester.destroy();
        tester=null;
      }
    }
  }
;
}
