{
  final ValueMap attributes=tag.getAttributes();
  final String attributeValue="javascript:validate('" + getCallbackUrl() + "', '"+ formComponent.getInput()+ "', this);";
  attributes.put(getEventName(),attributeValue);
}
