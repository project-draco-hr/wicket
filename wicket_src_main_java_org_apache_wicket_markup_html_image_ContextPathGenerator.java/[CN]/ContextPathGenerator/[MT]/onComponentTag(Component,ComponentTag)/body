{
  final String path=contextRelativePath.getObject();
  final String rewritten=component.getRequestCycle().getProcessor().getRequestCodingStrategy().rewriteStaticRelativeUrl(path);
  tag.put("src",rewritten);
}
