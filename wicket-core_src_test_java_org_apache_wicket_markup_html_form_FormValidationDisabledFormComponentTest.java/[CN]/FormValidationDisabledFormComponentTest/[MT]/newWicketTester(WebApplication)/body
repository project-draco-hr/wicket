{
  return new WicketTester(app){
    @Override public void clearFeedbackMessages(){
      cleanupFeedbackMessages(getApplication().getApplicationSettings().getFeedbackMessageCleanupFilter());
    }
  }
;
}
