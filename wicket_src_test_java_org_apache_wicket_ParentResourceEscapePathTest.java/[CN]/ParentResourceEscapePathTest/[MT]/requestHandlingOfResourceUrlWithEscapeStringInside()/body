{
  tester.getRequest().setUrl(Url.parse("wicket/resource/WicketTester$DummyWebApplication/WicketTester$DummyWebApplication/" + expectedResourceUrl()));
  tester.processRequest();
  tester.assertNoErrorMessage();
  assertEquals("// ParentResourceTest.js",tester.getLastResponseAsString());
}
