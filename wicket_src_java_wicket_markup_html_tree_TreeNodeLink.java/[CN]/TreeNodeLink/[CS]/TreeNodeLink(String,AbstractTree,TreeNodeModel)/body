{
  super(componentName);
  this.tree=tree;
  this.node=node;
  Object userObject=node.getTreeNode().getUserObject();
  String linkId=String.valueOf(userObject.hashCode());
  id=linkId;
  tree.addLink(this);
}
