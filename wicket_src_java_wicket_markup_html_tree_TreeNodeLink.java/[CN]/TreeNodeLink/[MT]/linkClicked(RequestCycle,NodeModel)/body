{
  Serializable userObject=node.getUserObject();
  TreeStateCache state=tree.getTreeState();
  TreePath selection=state.findTreePath(userObject);
  tree.setExpandedState(selection,(!node.isExpanded()));
}
