{
  if (tag.isExtendTag() || tag.isChildTag()) {
    new TransparentWebMarkupContainer(container,Component.AUTO_COMPONENT_PREFIX + tag.getId()).render(markupStream);
    return true;
  }
  return false;
}
