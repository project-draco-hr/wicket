{
  checkTag(tag,"input");
  checkAttribute(tag,"type","password");
  super.handleComponentTag(tag);
  if (isResetPassword()) {
    tag.put("value","");
  }
 else {
    tag.put("value",getModelObjectAsString());
  }
}
