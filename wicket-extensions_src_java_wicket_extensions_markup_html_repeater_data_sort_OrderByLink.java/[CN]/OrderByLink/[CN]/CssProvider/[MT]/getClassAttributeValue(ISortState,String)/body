{
  int dir=state.getPropertyState(property);
  if (dir == ISortState.ASCENDING) {
    return ascending;
  }
 else   if (dir == ISortState.DESCENDING) {
    return descending;
  }
 else {
    return none;
  }
}
