{
  error=super.decorate(error,validatable);
switch (getMode()) {
case MINIMUM:
    error.addKey("MaximumValidator");
  break;
case MAXIMUM:
error.addKey("MinimumValidator");
break;
}
return error;
}
