{
  int l1=((FeedbackMessage)o1).getLevel();
  int l2=((FeedbackMessage)o2).getLevel();
  if (l1 < l2) {
    return sign * -1;
  }
 else   if (l1 > l2) {
    return sign * 1;
  }
 else {
    return 0;
  }
}
