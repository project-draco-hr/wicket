{
  int level1=((FeedbackMessage)o1).getLevel();
  int level2=((FeedbackMessage)o2).getLevel();
  if (level1 < level2) {
    return sign * -1;
  }
 else   if (level1 > level2) {
    return sign;
  }
 else {
    return 0;
  }
}
