{
  while (true) {
    if ((current instanceof ComponentTag) && ((ComponentTag)current).isWicketTag() && ((ComponentTag)current).getName().equals(wicketTagName)) {
      return;
    }
    if (next() == null) {
      return;
    }
  }
}
