{
  if (signedIn.compareAndSet(false,true)) {
    boolean authenticated=authenticate(username,password);
    if (authenticated) {
      bind();
    }
 else {
      signedIn.set(false);
    }
  }
  return signedIn.get();
}
