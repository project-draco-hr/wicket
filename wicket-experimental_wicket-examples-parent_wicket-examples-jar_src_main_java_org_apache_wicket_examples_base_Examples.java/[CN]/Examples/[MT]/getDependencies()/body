{
  HeaderItem bootstrap=JavaScriptHeaderItem.forReference(Bootstrap.responsive());
  HeaderItem prettify=JavaScriptHeaderItem.forReference(Prettify.get());
  return Arrays.asList(bootstrap,prettify);
}
