{
  Univariate stats=new Univariate(this.duration);
  log.info("min: " + stats.min() + "; max: "+ stats.max()+ "; mean: "+ stats.mean()+ "; median: "+ stats.median()+ "; stdev: "+ stats.stdev()+ "; variance: "+ stats.variance());
}
