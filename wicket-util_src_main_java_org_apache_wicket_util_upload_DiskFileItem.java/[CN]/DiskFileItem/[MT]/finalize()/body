{
  super.finalize();
  File outputFile=dfos.getFile();
  if (outputFile != null && outputFile.exists()) {
    if (outputFile.delete() == false)     log.debug("failed to delete file: " + outputFile.getAbsolutePath());
  }
}
