{
  cachedContent=null;
  File outputFile=getStoreLocation();
  if (outputFile != null && outputFile.exists()) {
    if (outputFile.delete())     log.debug("failed to delete file: " + outputFile.getAbsolutePath());
  }
}
