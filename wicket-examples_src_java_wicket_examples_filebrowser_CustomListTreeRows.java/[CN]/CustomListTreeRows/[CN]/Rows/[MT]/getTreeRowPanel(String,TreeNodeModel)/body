{
  ListTreeRowReplacementModel replacementModel=new ListTreeRowReplacementModel(nodeModel);
  Panel rowPanel=new CustomListTreeRow(componentName,CustomListTreeRows.this.tree,nodeModel);
  rowPanel.add(new ComponentTagAttributeModifier("class",true,replacementModel));
  return rowPanel;
}
