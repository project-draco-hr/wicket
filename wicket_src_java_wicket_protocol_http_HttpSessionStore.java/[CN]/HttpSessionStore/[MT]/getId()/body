{
  if (id == null) {
    HttpSession httpSession=getHttpSession(false);
    if (httpSession != null) {
      id=httpSession.getId();
      return id;
    }
    return String.valueOf(hashCode());
  }
  return id;
}
