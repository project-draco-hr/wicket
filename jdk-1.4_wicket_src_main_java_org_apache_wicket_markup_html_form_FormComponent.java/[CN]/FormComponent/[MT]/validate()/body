{
  validateRequired();
  if (isValid()) {
    convertInput();
    if (isRequired() && getConvertedInput() == null) {
      reportRequiredError();
    }
    if (isValid()) {
      validateValidators();
    }
  }
}
