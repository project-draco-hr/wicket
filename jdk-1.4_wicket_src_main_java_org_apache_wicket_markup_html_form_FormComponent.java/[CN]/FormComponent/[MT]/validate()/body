{
  validateRequired();
  if (isValid()) {
    convertInput();
    if (isValid() && isRequired() && getConvertedInput() == null && !isInputNullable()) {
      reportRequiredError();
    }
    if (isValid()) {
      validateValidators();
    }
  }
}
