{
  final String value=getModelObjectAsString();
  if (value != null) {
    try {
      return getApplication().getSecuritySettings().getCryptFactory().newCrypt().encryptUrlSafe(value);
    }
 catch (    Exception e) {
      log.error("Problem applying encryption; go on without for now",e);
    }
  }
  return value;
}
