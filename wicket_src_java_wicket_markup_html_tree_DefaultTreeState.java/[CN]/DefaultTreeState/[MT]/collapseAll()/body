{
  if (nodes.isEmpty() && nodesCollapsed == false) {
  }
 else {
    nodes.clear();
    nodesCollapsed=false;
    for (    ITreeStateListener l : listeners) {
      l.allNodesCollapsed();
    }
  }
}
