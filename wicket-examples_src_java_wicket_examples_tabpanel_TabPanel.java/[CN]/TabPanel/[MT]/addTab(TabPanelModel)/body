{
  tabs.add(tab);
  if (tab.isSelected()) {
    if (tab.isEnabled()) {
      selectTab(indexOf(tab));
    }
 else {
      tab.setSelected(false);
    }
  }
  if ((selectedTab < 0) && tab.isEnabled()) {
    selectTab(indexOf(tab));
  }
}
