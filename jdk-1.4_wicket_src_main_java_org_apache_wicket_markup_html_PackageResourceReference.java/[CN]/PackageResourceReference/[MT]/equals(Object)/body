{
  if (obj instanceof PackageResourceReference) {
    PackageResourceReference that=(PackageResourceReference)obj;
    return checkEquals(this.getScope(),that.getScope()) && checkEquals(this.getName(),that.getName()) && checkEquals(this.getLocale(),that.getLocale())&& checkEquals(this.getStyle(),that.getStyle());
  }
  return false;
}
