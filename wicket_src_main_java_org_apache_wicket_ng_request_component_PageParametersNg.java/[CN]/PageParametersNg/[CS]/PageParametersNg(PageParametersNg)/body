{
  Checks.argumentNotNull(copy,"copy");
  if (copy.indexedParameters != null)   indexedParameters=new ArrayList<String>(copy.indexedParameters);
  if (copy.namedParameters != null)   namedParameters=new ArrayList<Entry>(copy.namedParameters);
}
