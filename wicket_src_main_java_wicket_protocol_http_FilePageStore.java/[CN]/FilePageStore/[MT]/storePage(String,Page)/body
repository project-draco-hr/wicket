{
synchronized (storePageMap) {
    storePageMap.put(new SessionPageKey(sessionId,page.getNumericId(),page.getCurrentVersionNumber(),page.getAjaxVersionNumber()),page);
    storePageMap.notifyAll();
  }
}
