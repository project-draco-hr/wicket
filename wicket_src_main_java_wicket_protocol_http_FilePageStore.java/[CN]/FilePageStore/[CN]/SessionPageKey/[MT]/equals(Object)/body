{
  if (obj instanceof SessionPageKey) {
    SessionPageKey key=(SessionPageKey)obj;
    return id == key.id && versionNumber == key.versionNumber && sessionId.equals(key.sessionId) && remove == key.remove;
  }
  return false;
}
