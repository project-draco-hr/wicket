{
  appliedChangeListForVersionNumber.put(new Integer(getCurrentVersionNumber() - 1),changeList);
  if (log.isDebugEnabled()) {
    log.debug("version " + versionNumber + " for page "+ page+ " stored");
  }
}
