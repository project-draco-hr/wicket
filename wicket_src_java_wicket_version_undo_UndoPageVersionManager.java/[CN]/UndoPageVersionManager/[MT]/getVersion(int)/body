{
  if (versionNumber <= getCurrentVersionNumber()) {
    while (getCurrentVersionNumber() > versionNumber) {
      if (!undo()) {
        return null;
      }
    }
    return page;
  }
 else {
    return null;
  }
}
