{
  if (!IInitializer.class.isAssignableFrom(appletCodeClass)) {
    throw new IllegalArgumentException("Applet initializer class " + appletCodeClass.getName() + " must implement "+ IInitializer.class.getName());
  }
  this.appletCodeClass=appletCodeClass;
  addClass(appletCodeClass);
}
