{
  List<String> gets=Arrays.asList("/app1/wicket/bookmarkable/org.apache.wicket.threadtest.apps.app1.Home","/app1/wicket/page?0-${iteration}.ILinkListener-link");
  SimpleGetCommand getCmd=new SimpleGetCommand(gets,10);
  Tester tester=new Tester(getCmd,100,true);
  tester.run();
}
