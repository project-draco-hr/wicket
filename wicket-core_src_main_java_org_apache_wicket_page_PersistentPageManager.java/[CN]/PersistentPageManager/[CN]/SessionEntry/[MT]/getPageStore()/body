{
  PersistentPageManager manager=managers.get(applicationName);
  if (manager == null) {
    throw new IllegalStateException("PageManager for application '" + applicationName + "' not registered.");
  }
  return manager.pageStore;
}
