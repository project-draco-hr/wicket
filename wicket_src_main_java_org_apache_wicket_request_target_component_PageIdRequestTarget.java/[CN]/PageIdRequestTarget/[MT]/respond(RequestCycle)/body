{
  Page page=requestCycle.getSession().getPage(id.getPageMapName(),"" + id.getPageNumber(),id.getPageVersion());
  if (requestCycle.isRedirect()) {
    requestCycle.redirectTo(page);
  }
 else {
    page.renderPage();
  }
}
