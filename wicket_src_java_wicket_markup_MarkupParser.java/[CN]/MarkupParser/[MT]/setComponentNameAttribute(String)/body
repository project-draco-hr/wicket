{
  this.componentNameAttribute=name;
  if (!ComponentTag.DEFAULT_COMPONENT_NAME_ATTRIBUTE.equals(componentNameAttribute)) {
    log.info("You are using a non-standard component name attribute: " + componentNameAttribute);
  }
}
