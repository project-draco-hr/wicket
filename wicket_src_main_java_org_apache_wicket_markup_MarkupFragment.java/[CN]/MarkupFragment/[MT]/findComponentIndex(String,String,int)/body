{
  path=(path == null ? rootPath : (rootPath == null ? path : rootPath + ":" + path));
  int index=markup.findComponentIndex(path,id,this.startIndex + startIndex) - this.startIndex;
  if ((index >= 0) && (index < size)) {
    return index;
  }
  return -1;
}
