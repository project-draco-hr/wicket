{
  if (this == obj) {
    return true;
  }
  if (obj instanceof SessionPageMapKey == false) {
    return false;
  }
  SessionPageMapKey rhs=(SessionPageMapKey)obj;
  return Objects.equal(sessionId,rhs.sessionId) && Objects.equal(pageMapName,rhs.pageMapName);
}
