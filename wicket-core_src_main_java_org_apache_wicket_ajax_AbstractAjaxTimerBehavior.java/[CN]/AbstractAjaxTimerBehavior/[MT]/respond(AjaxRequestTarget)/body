{
  if (shouldTrigger()) {
    onTimer(target);
    if (shouldTrigger()) {
      hasTimeout=false;
      addTimeout(target.getHeaderResponse());
      return;
    }
  }
  clearTimeout(target.getHeaderResponse());
}
