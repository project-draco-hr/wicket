{
  super.renderHead(component,response);
  if (component.getRequestCycle().find(AjaxRequestTarget.class) == null) {
    hasTimeout=false;
  }
  if (isStopped() == false) {
    addTimeout(response);
  }
}
