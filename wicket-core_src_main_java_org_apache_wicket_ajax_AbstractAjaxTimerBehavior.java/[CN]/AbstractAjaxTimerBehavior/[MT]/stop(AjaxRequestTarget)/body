{
  if (stopped == false) {
    stopped=true;
    String timeoutHandle=getTimeoutHandle();
    target.getHeaderResponse().render(OnLoadHeaderItem.forScript("clearTimeout(" + timeoutHandle + "); delete "+ timeoutHandle+ ";"));
  }
}
