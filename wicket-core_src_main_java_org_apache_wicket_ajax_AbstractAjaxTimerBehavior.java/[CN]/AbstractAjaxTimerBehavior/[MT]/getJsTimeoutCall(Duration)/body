{
  CharSequence ajaxAttributes=renderAjaxAttributes(getComponent());
  String timeoutHandle=getTimeoutHandle();
  return timeoutHandle + " = setTimeout('Wicket.Ajax.ajax(" + ajaxAttributes+ ");', "+ updateInterval.getMilliseconds()+ ")";
}
