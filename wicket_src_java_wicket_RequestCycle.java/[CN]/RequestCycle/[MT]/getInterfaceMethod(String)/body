{
  final Method method=(Method)listenerInterfaceMethods.get(interfaceName);
  if (method == null) {
    throw new RenderException("Attempt to access unknown interface " + interfaceName);
  }
  return method;
}
