{
  if (getRedirect()) {
    setRedirect(false);
    if (page != null) {
      page.messages=FeedbackMessages.get();
      FeedbackMessages.remove();
    }
 else {
      FeedbackMessages.release();
    }
  }
 else {
    FeedbackMessages.release();
  }
  current.set(null);
}
