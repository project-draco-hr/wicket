{
  if (getRedirect()) {
    setRedirect(false);
    if (page != null) {
      page.messages=UIMessages.get();
      UIMessages.remove();
    }
 else {
      UIMessages.release();
    }
  }
 else {
    UIMessages.release();
  }
  current.set(null);
}
