{
  if (getRedirect()) {
    setRedirect(false);
    if (page != null) {
      page.messages=FeedbackMessages.get();
      FeedbackMessages.remove();
    }
 else {
      FeedbackMessages.threadDetach();
    }
  }
 else {
    FeedbackMessages.threadDetach();
  }
  current.set(null);
}
