{
  IRequestHandler handler=new ResourceReferenceRequestHandler(AbstractDefaultAjaxBehavior.INDICATOR);
  return new Label(markupId,"<img src=\"" + RequestCycle.get().renderUrlFor(handler) + "\"/>").setEscapeModelStrings(false);
}
