{
  this(id,page.getPageMap(),new IPageLink(){
    private static final long serialVersionUID=1L;
    public Page getPage(){
      return page;
    }
    @SuppressWarnings("unchecked") public Class<? extends Page> getPageIdentity(){
      return page.getClass();
    }
  }
);
}
