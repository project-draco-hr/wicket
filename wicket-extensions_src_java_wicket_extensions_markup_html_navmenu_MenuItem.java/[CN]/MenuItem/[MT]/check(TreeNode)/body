{
  if (treeNode == null) {
    throw new NullPointerException("treeNode may not be null");
  }
  if (!(treeNode instanceof MenuItem)) {
    throw new IllegalArgumentException("argument must be of type " + MenuItem.class.getName() + " (but is of type "+ treeNode.getClass().getName()+ ")");
  }
}
