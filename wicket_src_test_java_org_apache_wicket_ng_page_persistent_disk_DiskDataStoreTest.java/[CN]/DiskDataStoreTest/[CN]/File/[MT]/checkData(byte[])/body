{
  Checks.argumentNotNull(data,"data");
  if (data.length != length) {
    return false;
  }
  if (first != data[0]) {
    return false;
  }
  if (last != data[data.length - 1]) {
    return false;
  }
  return true;
}
