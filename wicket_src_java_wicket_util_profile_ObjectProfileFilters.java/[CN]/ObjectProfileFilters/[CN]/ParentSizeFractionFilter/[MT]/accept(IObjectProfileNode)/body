{
  final IObjectProfileNode parent=node.parent();
  if (parent == null) {
    return true;
  }
 else   if (node.size() >= (m_threshold * parent.size())) {
    return true;
  }
 else {
    return false;
  }
}
