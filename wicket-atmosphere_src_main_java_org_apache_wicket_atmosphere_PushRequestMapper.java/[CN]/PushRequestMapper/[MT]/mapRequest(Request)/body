{
  if (request instanceof PushWebRequest) {
    PushWebRequest pushRequest=(PushWebRequest)request;
    return new AtmosphereRequestHandler(pushRequest.getPageKey(),pushRequest.getSubscriptions(),pushRequest.getEvent());
  }
  return null;
}
