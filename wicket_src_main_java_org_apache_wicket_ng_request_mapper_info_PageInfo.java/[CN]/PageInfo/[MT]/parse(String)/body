{
  if (Strings.isEmpty(src)) {
    return new PageInfo();
  }
 else {
    try {
      return new PageInfo(Integer.valueOf(src));
    }
 catch (    NumberFormatException e) {
      return null;
    }
  }
}
