{
  Locale locale=Session.get().getLocale();
  if (Locale.GERMAN.equals(locale)) {
    return "%d.%m.%Y";
  }
  if ("nl".equals(locale.getLanguage())) {
    return "%d-%m-%Y";
  }
  return "%Y/%m/%d";
}
