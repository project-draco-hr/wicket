{
  Locale locale=Session.get().getLocale();
  if (Locale.GERMAN.equals(locale)) {
    return "%d.%m.%Y";
  }
  if (new Locale("nl").equals(locale)) {
    return "%d-%m-%Y";
  }
  return "%Y/%m/%d";
}
