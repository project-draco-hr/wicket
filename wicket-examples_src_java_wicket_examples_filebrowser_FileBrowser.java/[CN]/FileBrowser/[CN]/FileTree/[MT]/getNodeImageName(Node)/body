{
  final String img;
  if (node.isRoot()) {
    img="folderopen.gif";
  }
 else   if (node.isLeaf()) {
    img="node.gif";
  }
 else {
    if (node.isExpanded()) {
      img="folderopen.gif";
    }
 else {
      img="folder.gif";
    }
  }
  return img;
}
