{
  super.onComponentTag(tag);
  tag.put("for",fc.getMarkupId());
  if (fc.isRequired()) {
    tag.append("class","required"," ");
  }
  if (!fc.isValid()) {
    tag.append("class","error"," ");
  }
}
