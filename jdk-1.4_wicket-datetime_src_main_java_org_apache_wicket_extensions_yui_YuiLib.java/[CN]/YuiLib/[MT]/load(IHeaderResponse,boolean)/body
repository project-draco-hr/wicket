{
  if (configureBasePath) {
    response.renderJavascript("YAHOO_config = { load: { base: 'resources/" + YuiLib.class.getName() + "/'} } ","YAHOO_config");
  }
  response.renderJavascriptReference(new JavascriptResourceReference(YuiLib.class,"yuiloader-beta.js"));
}
