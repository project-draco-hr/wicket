{
  notify(new INotifier<IComponentOnBeforeRenderListener>(){
    @Override public void notify(    IComponentOnBeforeRenderListener listener){
      listener.onBeforeRender(component);
    }
  }
);
}
