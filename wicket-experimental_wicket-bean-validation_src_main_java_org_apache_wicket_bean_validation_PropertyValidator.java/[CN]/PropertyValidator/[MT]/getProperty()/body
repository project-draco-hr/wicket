{
  if (property == null) {
    property=BeanValidationConfiguration.get().resolveProperty(component);
    if (property == null) {
      throw new IllegalStateException("Could not resolve Property from component: " + component + ". Either specify the Property in the constructor or use a model that works in combination with a "+ IPropertyResolver.class.getSimpleName()+ " to resolve the Property automatically");
    }
  }
  return property;
}
