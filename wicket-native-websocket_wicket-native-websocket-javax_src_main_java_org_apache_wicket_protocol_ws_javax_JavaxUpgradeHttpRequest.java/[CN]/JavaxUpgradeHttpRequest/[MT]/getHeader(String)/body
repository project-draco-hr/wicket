{
  String value=null;
  if (headers != null) {
    List<String> headerValues=headers.get(name);
    if (headerValues.isEmpty() == false) {
      value=headerValues.get(0);
    }
  }
  return value;
}
