{
  final String key;
  if (value > 1.0) {
    key=units + "s";
  }
 else {
    key=units;
  }
  return StringValue.valueOf(value,locale) + " " + ResourceBundle.getBundle(Duration.class.getName(),locale).getString(key);
}
