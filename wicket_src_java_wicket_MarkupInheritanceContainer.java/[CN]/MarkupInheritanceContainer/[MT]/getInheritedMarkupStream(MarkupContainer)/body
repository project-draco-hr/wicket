{
  while (parent instanceof MarkupInheritanceContainer) {
    parent=parent.getParent();
  }
  return getApplication().getMarkupCache().getMarkupStream(parent,parent.getMarkupStream().getContainerClass().getSuperclass());
}
