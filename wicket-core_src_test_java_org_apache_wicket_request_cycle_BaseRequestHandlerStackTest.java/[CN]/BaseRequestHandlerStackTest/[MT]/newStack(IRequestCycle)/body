{
  return new RequestHandlerStack(){
    @Override protected void respond(    IRequestHandler handler){
      handler.respond(requestCycle);
    }
    @Override protected void detach(    IRequestHandler handler){
      handler.detach(requestCycle);
    }
  }
;
}
