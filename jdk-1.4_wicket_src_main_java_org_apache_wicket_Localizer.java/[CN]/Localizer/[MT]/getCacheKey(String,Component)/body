{
  String cacheKey=key;
  if (component != null) {
    cacheKey+='-' + component.getPageRelativePath();
    Page page=component.findPage();
    if (page != null) {
      cacheKey+='-' + page.getClass().getName();
    }
  }
  return cacheKey;
}
