{
  if (container == null) {
    throw new IllegalArgumentException("Parameter 'container' must not be 'null'.");
  }
  final IMarkup markup=getMarkup(container,container.getClass());
  if (markup != IMarkup.NO_MARKUP) {
    return new MarkupStream(markup);
  }
  if (throwException == true) {
    throw new MarkupNotFoundException("Markup not found. Component class: " + container.getClass().getName() + " Enable debug messages for wicket.util.resource to get a list of all filenames tried");
  }
  return null;
}
