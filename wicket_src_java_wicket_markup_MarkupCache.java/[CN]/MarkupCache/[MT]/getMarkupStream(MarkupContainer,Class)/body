{
  final Markup markup=getMarkup(container,clazz);
  if (markup != Markup.NO_MARKUP) {
    return new MarkupStream(markup);
  }
 else {
    throw new WicketRuntimeException("Markup not found. Component class: " + (clazz != null ? clazz.getName() : container.getClass().getName()) + " Enable debug messages for wicket.util.resource.Resource to get a list of all filenames tried.");
  }
}
