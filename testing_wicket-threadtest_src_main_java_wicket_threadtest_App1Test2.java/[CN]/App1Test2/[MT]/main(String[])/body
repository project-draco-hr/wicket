{
  List<String> gets=Arrays.asList(new String[]{"/app1?wicket:bookmarkablePage=one:wicket.threadtest.apps.app1.Home"});
  SimpleGetCommand getCmd=new SimpleGetCommand(gets,5);
  Tester tester=new Tester(getCmd,100,false);
  tester.run();
}
