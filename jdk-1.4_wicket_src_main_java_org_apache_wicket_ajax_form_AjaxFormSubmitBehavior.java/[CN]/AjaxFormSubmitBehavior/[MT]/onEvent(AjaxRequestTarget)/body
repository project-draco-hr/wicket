{
  getForm().onFormSubmitted();
  if (!getForm().hasError()) {
    onSubmit(target);
  }
  if (form.findParent(Page.class) != null) {
    if (getForm().hasError()) {
      onError(target);
    }
  }
}
