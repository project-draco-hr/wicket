{
  if (Boolean.class.equals(type)) {
    @SuppressWarnings("unchecked") IConverter<C> converter=(IConverter<C>)CheckBoxConverter.INSTANCE;
    return converter;
  }
 else {
    return super.getConverter(type);
  }
}
