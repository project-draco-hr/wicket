{
  Class cursor=clazz;
  while (cursor != null) {
switch (scanOrder) {
case SUB_TO_SUPER:
      hierarchy.add(cursor);
    break;
case SUPER_TO_SUB:
  hierarchy.add(0,cursor);
break;
}
cursor=cursor.getSuperclass();
}
}
