{
  Revision revision=null;
  try {
    String header=extractLicenseHeader(file,0,14);
    revision=Diff.diff(propertiesLicenseHeader.split(LINE_ENDING),header.split(LINE_ENDING));
  }
 catch (  Exception e) {
    fail(e.getMessage());
  }
  return revision.size() == 0;
}
