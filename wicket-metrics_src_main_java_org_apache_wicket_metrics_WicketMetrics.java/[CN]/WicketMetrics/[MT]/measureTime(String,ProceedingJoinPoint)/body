{
  if (WicketMetrics.enabled) {
    Context context=getMetricRegistry().timer(PREFIX + name + renderClassName(joinPoint)).time();
    try {
      return joinPoint.proceed();
    }
  finally {
      stopQuietly(context);
    }
  }
 else {
    return joinPoint.proceed();
  }
}
