{
  if (resourceReference != null) {
    component.getApplication().getResourceReferenceRegistry().registerResourceReference(resourceReference);
    resource=resourceReference.getResource();
    if (resource instanceof PackageResource) {
    }
  }
}
