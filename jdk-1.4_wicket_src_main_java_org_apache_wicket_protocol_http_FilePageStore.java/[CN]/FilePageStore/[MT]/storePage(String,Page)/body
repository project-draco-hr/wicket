{
  List list=(List)pagesToBeSerialized.get(sessionId);
  if (list == null) {
    list=new LinkedList();
  }
synchronized (list) {
    list.add(new SessionPageKey(sessionId,page));
  }
  pagesToBeSerialized.put(sessionId,list);
  putPageVersion(sessionId,page.getPageMapName(),new PageVersions(page.getNumericId(),page.getCurrentVersionNumber(),page.getAjaxVersionNumber()));
}
