{
  List list=getPageVersions(sessionId,pageMapName,true);
synchronized (list) {
    list.remove(pv);
    list.add(pv);
    if (list.size() > getRecordPageVersionCount()) {
      list.remove(0);
    }
  }
}
