{
  Object loadedObject=load();
  this.setObject(loadedObject);
  if (log.isDebugEnabled()) {
    log.debug("loaded transient object " + loadedObject + " for "+ this+ ", requestCycle "+ RequestCycle.get());
  }
}
