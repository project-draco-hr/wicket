{
  if (!attached) {
    tempModelObject=load();
    attached=true;
    if (log.isDebugEnabled()) {
      log.debug("loaded transient object " + tempModelObject + " for "+ this+ ", requestCycle "+ RequestCycle.get());
    }
  }
  return tempModelObject;
}
