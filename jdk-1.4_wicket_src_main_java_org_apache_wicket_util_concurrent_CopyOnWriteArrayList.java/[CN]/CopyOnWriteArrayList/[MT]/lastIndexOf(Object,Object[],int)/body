{
  if (elem == null) {
    for (int i=len - 1; i >= 0; i--) {
      if (elementData[i] == null) {
        return i;
      }
    }
  }
 else {
    for (int i=len - 1; i >= 0; i--) {
      if (elem.equals(elementData[i])) {
        return i;
      }
    }
  }
  return -1;
}
