{
  if ((this instanceof Page) && (markupStream != null) && (markupStream.equalMarkup(this.markupStream) == false)) {
    if (getPage() != null) {
      getPage().setAllowReRender(false);
    }
  }
  this.markupStream=markupStream;
}
