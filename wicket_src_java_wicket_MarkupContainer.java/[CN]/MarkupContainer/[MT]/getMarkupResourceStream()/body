{
  ContainerInfo containerInfo=new ContainerInfo(this);
  IResourceStream markupResource=Application.get().getResourceSettings().getResourceStreamLocator().locate(getClass(),getClass().getName().replace('.','/'),containerInfo.getStyle(),containerInfo.getLocale(),containerInfo.getFileExtension());
  return markupResource;
}
