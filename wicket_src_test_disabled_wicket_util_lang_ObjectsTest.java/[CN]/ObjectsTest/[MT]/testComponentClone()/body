{
  PropertyModel<String> pm=new PropertyModel<String>(new TextField<String>("test",new Model<String>("test")),"modelObject");
  PropertyModel<String> pm2=(PropertyModel<String>)Objects.cloneModel(pm);
  assertTrue(pm.getObject() == pm2.getObject());
}
