{
  if (requestCycleProcessor == null) {
    requestCycleProcessor=new CompoundRequestCycleProcessor(new WebRequestEncoder(),new DefaultEventProcessorStrategy());
  }
  return requestCycleProcessor;
}
