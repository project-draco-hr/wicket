{
  if (encoder == null) {
    throw new IllegalArgumentException("page parameters encoder cannot be null");
  }
  getDefaultRequestCycleProcessor().getRequestEncoder().mountPath(path,new BookmarkablePageRequestTarget(pageMapName,bookmarkablePageClass,path,encoder));
}
