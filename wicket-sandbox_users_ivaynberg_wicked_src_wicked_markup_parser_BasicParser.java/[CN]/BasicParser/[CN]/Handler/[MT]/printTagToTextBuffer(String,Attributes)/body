{
  textBuffer.append("<").append(name);
  for (int i=0; i < attributes.getLength(); i++) {
    textBuffer.append(" ").append(attributes.getQName(i)).append("=\"").append(attributes.getValue(i)).append("\"");
  }
  textBuffer.append(">");
}
