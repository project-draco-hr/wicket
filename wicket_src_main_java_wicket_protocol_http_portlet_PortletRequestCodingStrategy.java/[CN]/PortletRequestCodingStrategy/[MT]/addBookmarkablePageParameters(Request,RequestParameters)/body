{
  final String requestString=request.getParameter(AbstractWebRequestCodingStrategy.BOOKMARKABLE_PAGE_PARAMETER_NAME);
  if (requestString != null) {
    parameters.setBookmarkablePageClass(requestString);
  }
}
