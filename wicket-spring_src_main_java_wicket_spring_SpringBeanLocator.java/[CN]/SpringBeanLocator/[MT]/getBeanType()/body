{
  if (beanTypeCache == null) {
    try {
      beanTypeCache=Class.forName(beanTypeName,true,Thread.currentThread().getContextClassLoader());
    }
 catch (    ClassNotFoundException e) {
      throw new RuntimeException("SpringBeanLocator could not find class [" + beanTypeName + "] needed to locate the ["+ ((beanId != null) ? (beanId) : ("bean id not specified"))+ "] bean",e);
    }
  }
  return beanTypeCache;
}
