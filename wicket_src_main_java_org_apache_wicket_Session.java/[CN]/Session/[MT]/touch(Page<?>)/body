{
  List<Page<?>> lst=touchedPages.get();
  if (lst == null) {
    lst=new ArrayList<Page<?>>();
    touchedPages.set(lst);
    lst.add(page);
  }
 else   if (!lst.contains(page)) {
    lst.add(page);
  }
}
