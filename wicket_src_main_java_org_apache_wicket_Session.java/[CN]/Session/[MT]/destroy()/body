{
  if (sessionStore != null) {
    sessionStore.invalidate(RequestCycle.get().getRequest());
    sessionStore=null;
  }
  if (pageManager != null) {
    pageManager.destroy();
    pageManager=null;
  }
}
