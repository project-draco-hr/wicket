{
  Checks.argumentNotNull(reference,"reference");
  Key key=new Key(reference.getScope().getName(),reference.getName(),reference.getLocale(),reference.getStyle(),reference.getVariation());
  if (map.containsKey(key) == false) {
    map.put(key,reference);
  }
}
