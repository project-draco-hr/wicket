{
  super.onComponentTag(tag);
  if (isLinkEnabled()) {
    if (tag.getName().equalsIgnoreCase("a")) {
      tag.put("href","#");
    }
    tag.put("onclick",getTriggerJavaScript());
  }
 else {
    disableLink(tag);
  }
}
