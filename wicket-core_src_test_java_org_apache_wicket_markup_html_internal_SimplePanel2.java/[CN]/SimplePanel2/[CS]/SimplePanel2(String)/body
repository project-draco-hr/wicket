{
  super(id);
  add(new Label("label1",new Model<String>("Inside InlineEnclosure and Panel: hello world")));
  add(new Label("label2",new Model<String>("Inside InlineEnclosure, Panel and another InlineEnclosure: hello again.")));
}
