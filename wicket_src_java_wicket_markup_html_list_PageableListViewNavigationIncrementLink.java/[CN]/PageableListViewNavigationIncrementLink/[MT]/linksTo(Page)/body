{
  int currentPage=pageableListView.getCurrentPage();
  if (((increment < 0) && (currentPage <= 0)) || ((increment > 0) && (currentPage >= (pageableListView.getPageCount() - 1)))) {
    return true;
  }
  return false;
}
