{
  if (clazz == null) {
    throw new IllegalArgumentException("argument clazz must be not null");
  }
  if (!Page.class.isAssignableFrom(clazz)) {
    throw new IllegalArgumentException("argument clazz must be of type " + Page.class.getName());
  }
  classes.add(clazz);
}
