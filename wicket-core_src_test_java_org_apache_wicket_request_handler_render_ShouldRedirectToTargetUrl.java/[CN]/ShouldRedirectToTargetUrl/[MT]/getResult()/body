{
  TestPageRenderer renderer=new TestPageRenderer(null);
  return renderer.shouldRedirectToTargetUrl(ajax.next(),redirectPolicy.next(),redirectToRender.next(),targetEqualsCurrentUrl.next(),newPageInstance.next(),pageStateless.next(),sessionTemporary.next());
}
