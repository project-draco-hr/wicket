{
  final Application application=Application.get();
  IMarkupLoader loader=new InheritedMarkupMarkupLoader(application).setParent(new HeaderCleanupMarkupLoader(application).setParent(new BaseMarkupLoader()));
  return loader.loadMarkup(container,markupResourceStream);
}
