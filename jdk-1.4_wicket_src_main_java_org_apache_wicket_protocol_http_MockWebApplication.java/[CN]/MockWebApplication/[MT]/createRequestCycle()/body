{
  final WebRequestCycle cycle=(WebRequestCycle)application.newRequestCycle(wicketRequest,wicketResponse);
  wicketSession=(WebSession)Session.findOrCreate();
  cycle.setAutomaticallyClearFeedbackMessages(false);
  return cycle;
}
