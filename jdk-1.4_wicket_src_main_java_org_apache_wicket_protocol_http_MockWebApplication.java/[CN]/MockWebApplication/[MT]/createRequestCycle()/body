{
  final WebRequestCycle cycle=(WebRequestCycle)requestCycleFactory.newRequestCycle(application,wicketRequest,wicketResponse);
  this.wicketSession=(WebSession)Session.findOrCreate();
  cycle.setAutomaticallyClearFeedbackMessages(false);
  return cycle;
}
