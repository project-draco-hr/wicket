{
  NLTreeRowReplacementModel replacementModel=new NLTreeRowReplacementModel(nodeModel);
  Panel rowPanel=new FileNLTreeRow(componentName,FileNLTreeRows.this.tree,nodeModel);
  rowPanel.add(new ComponentTagAttributeModifier("class",true,replacementModel));
  return rowPanel;
}
