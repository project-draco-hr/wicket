{
  if (count > 1)   throw new WicketRuntimeException("The model of Select component [" + getPath() + "] is not of type java.util.Collection, but more then one SelectOption component "+ "has been selected. Either remove the multiple attribute from the select tag or "+ "make the model of the Select component a collection");
}
