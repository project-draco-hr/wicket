{
  super.renderJavaScriptReference(Application.get().getAjaxSettings().getWicketEventReference());
  toJsResponse(new Runnable(){
    public void run(){
      getRealResponse().renderOnLoadJavaScript(javascript);
    }
  }
);
}
