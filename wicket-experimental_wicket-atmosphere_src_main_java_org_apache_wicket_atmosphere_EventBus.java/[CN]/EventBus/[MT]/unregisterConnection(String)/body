{
  PageKey pageKey=trackedPages.remove(trackingId);
  if (pageKey != null) {
    fireUnregistration(trackingId);
    if (log.isInfoEnabled()) {
      log.info("unregistering page {} for session {}",new Object[]{pageKey.getPageId(),pageKey.getSessionId()});
    }
  }
}
