{
  super.onComponentTagBody(component,markupStream,openTag);
  ((MarkupContainer)component).renderAssociatedMarkup(Panel.PANEL,"Markup for a panel component has to contain part '<wicket:panel>'");
}
