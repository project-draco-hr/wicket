{
  Component resolvedComponent=getParent().get(tag.getId());
  if (resolvedComponent != null && (getPage().wasRendered(resolvedComponent) || resolvedComponent.isAuto())) {
    return null;
  }
  return resolvedComponent;
}
