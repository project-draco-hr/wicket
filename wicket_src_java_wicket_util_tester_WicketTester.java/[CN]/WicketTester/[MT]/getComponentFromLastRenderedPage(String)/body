{
  Component component=getLastRenderedPage().get(path);
  if (component == null) {
    Assert.fail("path: '" + path + "' does no exist for page: "+ getLastRenderedPage().getClass().getSimpleName());
  }
  return component;
}
