{
  if (!getLastRenderedPage().getClass().isAssignableFrom(expectedReneredPageClass)) {
    Assert.assertEquals(Classes.name(expectedReneredPageClass),Classes.name(getLastRenderedPage().getClass()));
  }
}
