{
  Component component=getComponentFromLastRenderedPage(path);
  Assert.assertTrue("componet '" + Classes.name(component.getClass()) + "' is not type:"+ Classes.name(componentClass),componentClass.isAssignableFrom(component.getClass()));
}
