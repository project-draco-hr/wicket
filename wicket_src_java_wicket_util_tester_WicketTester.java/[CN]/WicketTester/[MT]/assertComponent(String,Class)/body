{
  Component component=getComponentFromLastRenderedPage(path);
  Assert.assertTrue("componet '" + component.getClass().getSimpleName() + "' is not type:"+ componentClass.getSimpleName(),componentClass.isAssignableFrom(component.getClass()));
}
