{
  this.node=node;
  this.parent=parent;
  children=new ArrayList<TreeNode>();
  if (!(node instanceof Component) && !(node instanceof Behavior))   throw new IllegalArgumentException("Only accepts Components and Behaviors");
}
