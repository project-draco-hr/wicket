{
  return (getApplication().getSecuritySettings().getAuthorizationStrategy().allow(IAuthorizationStrategy.ACTION_ENABLED_STATE,FormComponent.this) && FormComponent.this.isEnabled()) ? null : "disabled";
}
