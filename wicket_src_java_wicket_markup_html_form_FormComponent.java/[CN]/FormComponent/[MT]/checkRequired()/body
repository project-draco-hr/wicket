{
  if (isRequired()) {
    final String input=getInput();
    if (input == null && !isInputNullable()) {
      return true;
    }
    if (Strings.isEmpty(input)) {
      return false;
    }
  }
  return true;
}
