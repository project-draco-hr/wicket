{
  if (this instanceof ICookieValue) {
    this.persistenceEnabled=persistenceEnabled;
  }
 else {
    throw new UnsupportedOperationException("FormComponent " + getClass().getName() + " does not support cookies");
  }
}
