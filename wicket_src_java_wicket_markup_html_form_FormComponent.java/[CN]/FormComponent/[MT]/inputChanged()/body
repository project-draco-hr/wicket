{
  if (isVisibleInHierarchy()) {
    final String[] input=inputAsStringArray();
    if (input != null) {
      rawInput=StringList.valueOf(input).join(";");
    }
 else {
      rawInput=null;
    }
  }
}
