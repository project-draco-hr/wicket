{
  try {
    this.xmlReader=new XmlReader(new BufferedInputStream(in,4000),"UTF-8");
    this.input=new FullyBufferedReader(this.xmlReader);
  }
  finally {
    in.close();
    if (this.xmlReader != null)     this.xmlReader.close();
  }
}
