{
  return new Link(id){
    /** 
 * @see wicket.markup.html.link.Link#onClick()
 */
    public void onClick(){
      item.modelChanging();
      getList().remove(item.getModelObject());
      item.modelChanged();
    }
  }
;
}
