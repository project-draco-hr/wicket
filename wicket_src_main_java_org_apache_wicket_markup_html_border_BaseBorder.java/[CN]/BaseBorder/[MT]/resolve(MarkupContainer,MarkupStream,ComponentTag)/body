{
  if (rendering == false) {
    if (BODY_ID.equals(tag.getId())) {
      rendering=true;
      try {
        body.render(markupStream);
      }
  finally {
        rendering=false;
      }
      return true;
    }
  }
  return false;
}
