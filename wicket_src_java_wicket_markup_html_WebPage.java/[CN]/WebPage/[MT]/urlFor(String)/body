{
  RequestCycle requestCycle=getRequestCycle();
  String url=requestCycle.getRequestCycleProcessor().getRequestEncoder().encode(requestCycle,new SharedResourceRequestTarget(resourceKey));
  return url;
}
