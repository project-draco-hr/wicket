{
  RequestCycle requestCycle=getRequestCycle();
  String url=requestCycle.getRequestCycleProcessor().getRequestEncoder().encode(requestCycle,new PageClassRequestTarget(pageMapName,pageClass,parameters));
  return url;
}
