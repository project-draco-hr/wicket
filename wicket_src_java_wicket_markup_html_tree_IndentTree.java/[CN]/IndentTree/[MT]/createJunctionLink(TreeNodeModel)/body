{
  TreeNodeLink junctionLink=new TreeNodeLink("junctionLink",this,node){
    public void linkClicked(    RequestCycle cycle,    TreeNodeModel node){
      junctionLinkClicked(cycle,node);
    }
  }
;
  HtmlContainer junctionImg=new HtmlContainer("junctionImg");
  junctionImg.add(new ComponentTagAttributeModifier("src",true,new Model(getJunctionImageName(node))));
  junctionLink.add(junctionImg);
  return junctionLink;
}
