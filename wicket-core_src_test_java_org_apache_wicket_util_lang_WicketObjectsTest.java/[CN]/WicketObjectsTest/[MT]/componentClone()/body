{
  PropertyModel<String> pm=new PropertyModel<String>(new TextField<>("test",Model.of("test")),"modelObject");
  PropertyModel<String> pm2=WicketObjects.cloneModel(pm);
  assertTrue(pm.getObject() == pm2.getObject());
}
