{
  final ResourceReference ref=new ResourceReference(ParentResourceEscapePathTestPage.class,"../../../ParentResourceTest.js");
  assertEquals(expectedResourceUrl(),tester.createRequestCycle().urlFor(ref).toString());
}
