{
  if (getApplication().getRequestCycleSettings().getRenderStrategy() != RenderStrategy.REDIRECT_TO_BUFFER) {
    preventDirty(true);
  }
  try {
    ++renderCount;
    render();
  }
  finally {
    preventDirty(false);
  }
}
