{
  ServletContext sc=webapp.getWicketServlet().getServletContext();
  final ApplicationContext ctx=WebApplicationContextUtils.getRequiredWebApplicationContext(sc);
  webapp.setMetaData(CONTEXT_KEY,new ApplicationContextHolder(ctx));
  InjectorHolder.setInjector(new AnnotSpringInjector(new ContextLocator()));
}
