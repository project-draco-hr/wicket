{
  SessionFacade sessionFacade=getApplication().getSessionFacade();
  RequestCycle requestCycle=RequestCycle.get();
  return sessionFacade.getSessionStore(requestCycle != null ? requestCycle.getRequest() : null);
}
