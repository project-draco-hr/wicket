{
  if (log.isDebugEnabled()) {
    log.debug("Getting page [path = " + path + ", versionNumber = "+ versionNumber+ "]");
  }
  Page page=getPage(pageMapName,Integer.parseInt(Strings.firstPathComponent(path,COMPONENT_PATH_SEPERATOR)));
  if (page != null) {
    final Page version=page.getVersion(versionNumber);
    if (version != null) {
      if (version != page) {
        page=version;
        page.getPageMap().put(page);
      }
      return page;
    }
  }
  return null;
}
