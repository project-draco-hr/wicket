{
  final Session session=current.get();
  if (session == null) {
    throw new WicketRuntimeException("there is no session attached to current thread " + Thread.currentThread().getName());
  }
  return session;
}
