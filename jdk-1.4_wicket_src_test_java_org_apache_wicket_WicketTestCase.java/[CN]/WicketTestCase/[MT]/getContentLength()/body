{
  String contentLength=((MockHttpServletResponse)tester.getWicketResponse().getHttpServletResponse()).getHeader("Content-Length");
  if (contentLength == null)   throw new WicketRuntimeException("No Content-Length header found");
  return Integer.parseInt(contentLength);
}
