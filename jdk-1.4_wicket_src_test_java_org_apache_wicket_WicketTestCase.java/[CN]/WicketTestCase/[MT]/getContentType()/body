{
  String contentType=((MockHttpServletResponse)tester.getWicketResponse().getHttpServletResponse()).getHeader("Content-Type");
  if (contentType == null)   throw new WicketRuntimeException("No Content-Type header found");
  return contentType;
}
