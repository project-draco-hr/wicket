{
  if (nodes.isEmpty() && nodesCollapsed == true) {
  }
 else {
    nodes.clear();
    nodesCollapsed=true;
    for (    ITreeStateListener listener : listeners)     listener.allNodesExpanded();
  }
}
