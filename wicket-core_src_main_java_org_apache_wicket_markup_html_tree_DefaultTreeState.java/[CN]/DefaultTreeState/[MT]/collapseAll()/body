{
  if (nodes.isEmpty() && nodesCollapsed == false) {
  }
 else {
    nodes.clear();
    nodesCollapsed=false;
    for (    ITreeStateListener listener : listeners)     listener.allNodesCollapsed();
  }
}
