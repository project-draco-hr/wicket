{
  attach();
  try {
    onSetObject(component,object);
  }
 catch (  RuntimeException e) {
    throw new WicketRuntimeException("unable to set object " + object + ", model: "+ this+ ", called with component "+ component,e);
  }
}
