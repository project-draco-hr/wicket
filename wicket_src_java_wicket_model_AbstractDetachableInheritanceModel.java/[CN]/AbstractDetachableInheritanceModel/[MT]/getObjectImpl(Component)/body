{
  attach();
  try {
    return onGetObject(component);
  }
 catch (  RuntimeException e) {
    throw new WicketRuntimeException("unable to get object, model: " + this + ", called with component "+ component,e);
  }
}
