{
  super.onBind();
  if (!(getComponent() instanceof AbstractTextComponent)) {
    throw new WicketRuntimeException("Behavior " + getClass().getName() + " can only be added to an instance of a AbstractTextComponent");
  }
}
