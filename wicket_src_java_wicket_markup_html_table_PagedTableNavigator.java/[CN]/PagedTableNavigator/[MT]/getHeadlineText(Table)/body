{
  int firstCell=table.getCurrentPage() * table.getRowsPerPage();
  StringBuffer buf=new StringBuffer(80);
  buf.append(String.valueOf(table.getList().size())).append(" items found, displaying ").append(String.valueOf(firstCell + 1)).append(" to ").append(String.valueOf(firstCell + table.getRowsPerPage())).append(".");
  return buf.toString();
}
