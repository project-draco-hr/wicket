{
  if ((value == null) || (value instanceof String) || (value instanceof String[])) {
    return super.put(key,value);
  }
 else {
    throw new IllegalArgumentException("You tried to add an object of type " + value.getClass().getName() + "to your PageParameters for key "+ key+ ", but you are only allowed to use String or String[].");
  }
}
