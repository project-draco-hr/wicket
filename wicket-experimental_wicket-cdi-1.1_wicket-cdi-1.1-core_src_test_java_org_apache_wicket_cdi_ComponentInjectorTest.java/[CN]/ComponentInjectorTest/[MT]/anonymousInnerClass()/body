{
  WebComponent component=new WebComponent("someId"){
    @Inject private String dependency;
    @Override public String toString(){
      return dependency;
    }
  }
;
  componentInjector.onInstantiation(component);
  assertNull(component.toString());
}
