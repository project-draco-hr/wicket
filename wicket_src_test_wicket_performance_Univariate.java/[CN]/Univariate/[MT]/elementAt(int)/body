{
  double element=0;
  try {
    element=x[index];
  }
 catch (  final ArrayIndexOutOfBoundsException e) {
    System.out.println("Index " + index + " does not exist in data.");
  }
  return (element);
}
