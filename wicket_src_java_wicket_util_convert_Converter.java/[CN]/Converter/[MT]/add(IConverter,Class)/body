{
  if (converter == null || c == null) {
    throw new IllegalArgumentException("parameters converter and c(lass( must be not-null");
  }
  IConverter old=(IConverter)converters.put(c,converter);
  return old;
}
