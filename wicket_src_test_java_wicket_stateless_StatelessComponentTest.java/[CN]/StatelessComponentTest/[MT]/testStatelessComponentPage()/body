{
  executeTest(StatelessComponentPage.class,"StatelessComponentPage_result.html");
  application.setupRequestAndResponse();
  application.getServletRequest().setURL("?wicket:bookmarkablePage=:wicket.stateless.StatelessComponentPage&wicket:interface=:0:link::ILinkListener:");
  try {
    application.processRequestCycle();
    assertTrue(false);
  }
 catch (  Exception e) {
    assertEquals("wanted exception",e.getMessage());
  }
}
