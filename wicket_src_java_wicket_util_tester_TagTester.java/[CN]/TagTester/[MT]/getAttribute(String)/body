{
  String value=null;
  if (openTag.hasAttributes()) {
    AttributeMap attributeMap=openTag.getAttributes();
    for (    String attr : attributeMap.keySet()) {
      if (attr.equalsIgnoreCase(attribute)) {
        value=attributeMap.getString(attr);
      }
    }
  }
  return value;
}
