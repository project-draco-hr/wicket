{
  ResourceReference reference=getResourceReference(scope,name,locale,style,variation,strict,false);
  if (reference == null) {
    scanner.scanClass(scope);
  }
  reference=getResourceReference(scope,name,locale,style,variation,strict,true);
  return reference;
}
