{
  Class<?> clazz;
  clazz=WicketObjects.resolveClass(type);
  if (clazz == null) {
    throw new InvalidClassException(type,"could not resolve class [" + type + "] when deserializing proxy");
  }
  return LazyInitProxyFactory.createProxy(clazz,locator);
}
