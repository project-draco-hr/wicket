{
  if (nodes.isEmpty() && nodesCollapsed == false) {
  }
 else {
    nodes.clear();
    nodesCollapsed=false;
    for (    TreeStateListener l : listeners) {
      l.allNodesCollapsed();
    }
  }
}
