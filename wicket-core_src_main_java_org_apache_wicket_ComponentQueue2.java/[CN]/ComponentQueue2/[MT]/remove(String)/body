{
  if (queue != null) {
    Component removed=queue.remove(id);
    if (removed != null) {
      queueSize--;
      if (isEmpty()) {
        queue=null;
      }
      return removed;
    }
  }
  return null;
}
