{
  if (log.isDebugEnabled()) {
    log.debug("UNDO ADD: removing " + component.getPath() + " ("+ Classes.name(component.getClass())+ "@"+ component.hashCode()+ ") from parent");
  }
  component.remove();
}
